<!--
  Delegation task template for SpeakType.

  HOW TO USE (per AGENTS.md):
  1. Copy this file to `.task-<name>.md` at the repo root.
  2. Fill every <…> placeholder. Pull the "Load first" list from `context-map.md`
     (or the relevant `plan/phase-N.md` → "## Load first" block).
  3. Invoke the agent with a SHORT command, e.g.:
       pi -a -p "Read ./.task-<name>.md and execute it fully."
  4. DELETE `.task-<name>.md` when the task is done.

  Keep the read-list minimal — only the files the task actually needs.
  Do not paste file contents here; the agent has repo access and will read them.
-->

# Task: <one-line title>

## Load first (read these, in order, before doing anything)
- Phase file: `plan/phase-<N>-<name>.md`
- Skill(s): `.claude/skills/<skill>/SKILL.md`
- Memory: `memory/<file>.md`, `memory/<file>.md`
- Contract (if touching the API boundary): `packages/shared/src/{api-contract,schemas,constants}.ts`
- Design (if any UI): `DESIGN.md`

## Goal
<what "done" looks like, in one or two sentences>

## Scope
- In scope: <files / folders to change>
- Out of scope: <what NOT to touch>

## Constraints (non-negotiable)
- Zero-cost MVP — flag any paid step before doing it; do not wire a paid service.
- Locked decisions — do not swap any provider/library (see `memory/stack-decisions.md`).
- Preserve UTF-8 encoding; never corrupt Arabic text.
- Secrets stay server-side; the extension never holds Groq/Gemini/DB keys.
- DB changes: drizzle generate → migrate, **never push**; UUID PKs for non-auth tables.

## Steps
1. <step>
2. <step>

## Verify (must pass before reporting done)
- `pnpm typecheck` and `pnpm lint` are clean.
- <task-specific check, e.g. route returns 401 without Bearer; icon shows correct states>
- Report what you changed, what you ran, and the actual output — do not assume it works.
